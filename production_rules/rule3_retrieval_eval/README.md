# Rule 3 — Test retrieval separately from generation

Measures Recall@k, MRR, nDCG, exact-article hit, repealed-leak (must be 0),
ACL-leak (must be 0) against `gold/retrieval_gold.jsonl`.

Script `run_retrieval_eval.py` supplied by brief author.
