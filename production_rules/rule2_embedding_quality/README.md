# Rule 2 — Chunk quality > quantity

Fails if any chunk represents more than one citable unit, if boilerplate
leaks, or if near-duplicates exceed threshold.

Script `check_corpus_quality.py` supplied by brief author.
